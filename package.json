{
  "name": "xml_normalize",
  "version": "0.3.0",
  "description": "Normalizes xml files, by sorting siblings based on provided attribute and applying indentation.",
  "main": "dist/index.js",
  "scripts": {
    "start": "tsc && node dist/index.js --help",
    "run-examples": "tsc && node dist/index.js -i examples/messages.xlf -o examples/messages2.xlf -s xliff.file[0].unit.id -r xliff.file[].unit[].notes -n",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "xml",
    "pretty",
    "sort",
    "normalize"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/daniel-sc/xml_normalize.git"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "author": "daniel-schreiber@gmx.de",
  "license": "ISC",
  "devDependencies": {
    "@types/commander": "~2.12.2",
    "@types/xml2js": "~0.4.8",
    "tslint": "~6.1.3",
    "typescript": "~4.2.3"
  },
  "dependencies": {
    "commander": "~7.1.0",
    "xml2js": "~0.4.23"
  }
}
